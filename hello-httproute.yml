apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: hello
spec:
  hostnames:
  - hello.stage.local
  parentRefs:
  - group: gateway.networking.k8s.io
    kind: Gateway
    name: eg
    namespace: default
  rules:
  - backendRefs:
    - group: ""
      kind: Service
      name: hello
      port: 80
      weight: 1
    matches:
    - path:
        type: PathPrefix
        value: /


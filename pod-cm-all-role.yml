apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: pod-cm-all
  namespace: stage
rules:
- apiGroups:
  - ""
  resources:
  - pods
  - pods/attach
  - pods/log
  - pods/exec
  - configmap
  verbs:
  - '*'
